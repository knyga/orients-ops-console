# dependencies
node_modules

# next.js
.next

# env / secrets
.env
.env*.local

# typescript
*.tsbuildinfo

# slack raw mirror (re-syncable from Slack; PII + volume)
data/slack/

# personal field metrics export (financial PII — never commit)
docs/Personal Field Metrics */

# macOS junk
.DS_Store

# Local Claude Code settings (per-machine; never commit)
.claude/settings.local.json
